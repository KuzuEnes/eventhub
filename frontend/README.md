# EventHub Frontend

This is a Vite + React + TypeScript frontend scaffold for EventHub.

Quick start:

1. cd frontend
2. npm install
3. copy `.env.example` to `.env` and set `VITE_API_URL` if needed
4. npm run dev
